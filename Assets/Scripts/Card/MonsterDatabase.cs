using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonsterDatabase : MonoBehaviour
{
    //public Dictionary<string, CardID> cardDatabase;
    public void Awake() //when game starts build cards
    {
        //cardDatabase.Add(new Card(0, "Skeleton", 1, "Monster", "Undead", "Rattles a lot"));
        //cardDatabase.Add(new Card(1, "Abomination", 4, "Monster", "Undead", "Large, Loud, and Angry"));
        //cardDatabase.Add(new Card(2, "Bone Golem", 6, "Monster", "Undead", "Watch the pointy bits"));
        
    }
}
